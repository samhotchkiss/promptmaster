api_version = "1"
name = "default_launch_planner"
kind = "launch_planner"
version = "0.1.0"
entrypoint = "plugin.py:plugin"
description = "Built-in default launch planner — preserves historical Supervisor planning behavior."

[[capabilities]]
kind = "launch_planner"
name = "default"
requires_api = ">=1,<2"
