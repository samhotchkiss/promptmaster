api_version = "1"
name = "magic"
kind = "agent_profile"
version = "0.2.0"
entrypoint = "plugin.py:plugin"
description = "itsalive.co deployment integration with async verification and agent capability injection."

[[capabilities]]
kind = "agent_profile"
name = "magic"
requires_api = ">=1,<2"

[content]
kinds = ["magic_skill"]
user_paths = ["skills"]
